{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="d-flex justify-content-end">
        <button class="btn btn-secondary mt-5">{{ button_label|default('Rechercher') }}</button>
    </div>
{{ form_end(form) }}
