{% extends 'base_back.html.twig' %}

{% block title %}PA EXPRESS{% endblock %}

{% block body %}
<div class="p-3">
    <h1>Nouvel utilisateur</h1>
    {% for message in app.flashes('green') %}
            <div class="alert alert-success">
                {{ message }}
            </div>
        {% endfor %}
<div class="p-4 mt-4 card w-75">
<div class="">
    {{ include('Back/user/_form.html.twig') }}
</div>
</div>
</div>
{% endblock %}
