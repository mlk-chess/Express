<form method="post" action="{{ path('admin_train_delete', {'id': train.id}) }}" onsubmit="return confirm('Voulez-vous supprimer cette ligne ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ train.id) }}">
    <button class="mx-2 btn btn-danger">Supprimer</button>
</form>
