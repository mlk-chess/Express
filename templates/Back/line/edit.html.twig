{% extends 'base_back.html.twig' %}

{% block title %}Edit Line{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between">
        <h1>Modification</h1>
        <div>
            <a class="btn btn-primary" href="{{ path('admin_line_index') }}">Retour</a>
        </div>
    </div>

    {{ include('Back/line/_form.html.twig', {'button_label': 'Modifier'}) }}


{% endblock %}
