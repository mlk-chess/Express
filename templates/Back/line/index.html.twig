{% extends 'base_back.html.twig' %}

{% block title %}Lignes{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between mb-5">
        <h1>Les lignes</h1>
        <div>
            <a class="btn btn-primary" href="{{ path('admin_line_new') }}">Créer une nouvelle ligne</a>
        </div>
    </div>

    <table id="table_line" class=" display">
        <thead>
            <tr>
                <th>Id</th>
                <th>Gare de départ</th>
                <th>Gare d'arrivée</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for line in lines %}
            <tr>
                <td>{{ line.id }}</td>
                <td>{{ line.nameStationDeparture }}</td>
                <td>{{ line.nameStationArrival }}</td>
                <td>
                    <a class="btn btn-primary" href="{{ path('admin_line_show', {'id': line.id}) }}">Voir</a>
                    <a class="btn btn-warning" href="{{ path('admin_line_edit', {'id': line.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">Aucune donnée</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

     <script>

        $(document).ready( function () {
            $('#table_line').DataTable();
        } );
    </script>
{% endblock %}
