<form method="post" action="{{ path('admin_line_delete', {'id': line.id}) }}" onsubmit="return confirm('Voulez-vous supprimer cette ligne ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ line.id) }}">
    <button class="btn">Supprimer</button>
</form>
