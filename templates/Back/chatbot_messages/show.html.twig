{% extends 'base_back.html.twig' %}

{% block title %}Messages - Admin{% endblock %}

{% block body %}
    <h1>ChatbotMessages</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chatbot_message.id }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ chatbot_message.message }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-outline-primary" href="{{ path('app_chatbot_messages_index') }}">Retourner à la liste des messages</a>


    {{ include('Back/chatbot_messages/_delete_form.html.twig') }}
{% endblock %}
