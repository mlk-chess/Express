{% extends 'base_back.html.twig' %}

{% block title %}Wagon index{% endblock %}

{% block body %}
    <h1>Wagon index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Classe</th>
                <th>Type</th>
                <th>PlaceNb</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for wagon in wagons %}
            <tr>
                <td>{{ wagon.id }}</td>
                <td>{{ wagon.class }}</td>
                <td>{{ wagon.type }}</td>
                <td>{{ wagon.placeNb }}</td>
                <td>
                    <a href="{{ path('admin_wagon_edit', {'id': wagon.id}) }}">Editer</a>
                    {{ include('Back/wagon/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">Pas de wagon enregistré</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_wagon_new') }}">Nouveau wagon</a>
{% endblock %}
