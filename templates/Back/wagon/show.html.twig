{% extends 'base_back.html.twig' %}

{% block title %}Wagon{% endblock %}

{% block stylesheets %}

    <style>
    .title{ 
        font-weight:bold !important;
        font-size:20px;
    }

    .subtitle{
        font-size:14px;
    }
    </style>
{% endblock%}

{% block body %}
<div class="p-3">
    <h1>Wagon</h1>


    <div class="card p-4 mt-4 w-75">
        
        <div class="">
            <div class="title">Classe</div>
            <div class="subtitle">{{ wagon.class }}</div>
        </div>
        <hr>
        <div class="">
            <div class="title">Type</div>
            <div class="subtitle">{{ wagon.type }}</div>
        </div>
        <hr>
        <div class="">
            <div class="title">Nombre de places</div>
            <div class="subtitle">{{ wagon.placeNb }}</div>
        </div>
    

        
    </div>

    <div class="d-flex mt-4">
        <form method="post" action="{{ path('admin_wagon_edit', {'id': wagon.id}) }}">
            <input type="hidden" name="_token" value="{{ csrf_token('edit' ~ wagon.id) }}">
            <button class="btn btn-primary-express">Modifier</button>
        </form>



        <div class="mx-2">
   
            <a class="btn btn-primary-express mx-2"href="{{ path('admin_wagon_disable', {'id': wagon.id}) }}">Désactiver ce wagon</a>
       
        </div>
    </div>

</div>
  
    
{% endblock %}
