{% extends 'base.html.twig' %}

{% block title %}Edit Offer{% endblock %}

{% block body %}
    <h1>Edit Offer</h1>

    {{ include('Back/offer/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('admin_offer_index') }}">back to list</a>

    {{ include('Back/offer/_delete_form.html.twig') }}
{% endblock %}
