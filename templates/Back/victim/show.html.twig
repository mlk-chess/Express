{% extends 'base.html.twig' %}

{% block title %}Victim{% endblock %}

{% block body %}
    <h1>Victim</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ victim.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ victim.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ victim.lastname }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ victim.age }}</td>
            </tr>
            <tr>
                <th>Weigth</th>
                <td>{{ victim.weigth }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_victim_index') }}">back to list</a>

    <a href="{{ path('admin_victim_edit', {'id': victim.id}) }}">edit</a>

    {{ include('Back/victim/_delete_form.html.twig') }}
{% endblock %}
