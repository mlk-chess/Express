{#<form method="post" action="{{ path('delete_booking', {'id': booking.id}) }}" >
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ booking.id) }}">#}
    <button class="btn btn-danger"  data-bs-toggle="modal" data-bs-target="#infoModal" onclick="addContentRefund()">Annuler</button>
{#</form>#}
