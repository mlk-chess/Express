{% extends 'base.html.twig' %}

{% block title %}LigneTrain{% endblock %}

{% block body %}
    <h1>LigneTrain</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ligne_train.id }}</td>
            </tr>
            <tr>
                <th>HoraireA</th>
                <td>{{ ligne_train.horaireA ? ligne_train.horaireA|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>HoraireD</th>
                <td>{{ ligne_train.horaireD ? ligne_train.horaireD|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_ligne_train_index') }}">back to list</a>

    <a href="{{ path('admin_ligne_train_edit', {'id': ligne_train.id}) }}">edit</a>

    {{ include('Back/ligne_train/_delete_form.html.twig') }}
{% endblock %}
